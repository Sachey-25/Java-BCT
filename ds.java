DataStructures -- as the name suggesting DS is called organising the data in memory.
----------------
1. Arrays
2. Linked List
3. Stack
4. Queue
5. Tree

two types DS ===> 1. Primitive Data structure 2. Non-primitive data-structure.

Operations DS ===> Searching, Sorting, Insertion , Updation, Deletion.

Why? ===> Efficient, Reusuability, Abstraction

******************************************************************************************
******************************************************************************************

/*
	 * properties of arrays
	 * 1. Each element in an array is pf the same data type and carries the same size 
	 * that is 4bytes
	 * 2. Elements in the array arre stored at contiguous memory locations from which
	 * the first element is stored at the smallest memory location
	 * 3. Elements of the array can be randomly accessed since we can calculatre the address
	 * of each element of the arrway with the given base address.
	 * 
	 */
	 
	/*
		 * Representation of the array 
		 * ****************************************************
		 * <ReturnType>[Size] = {<values/data/elements>};
		 * ****************************************************
		 * 
		 */
---> Index starts from 0
---> The array's lenght is 10 which means we can store 10 elements 
---> Each element in the array can be accessed via its index.

***********************************************************************
Stack
Queue

>> Java-8 ---> 8different --

>> JDBC -- Spring framework. 

-------------------------------------------***************-----------------------------------------